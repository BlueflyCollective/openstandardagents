ossaVersion: "1.0"
agent:
  id: incident-response-coordinator
  name: Incident Response Coordinator
  version: 1.0.0
  description: "OSSA-compliant orchestrator agent: incident-response-coordinator"
  role: custom
  tags: []
  runtime:
    type: docker
  capabilities:
    - name: process_request
      description: Process incoming requests
      input_schema:
        type: object
      output_schema:
        type: object
  monitoring:
    traces: true
    metrics: true
    logs: true
